This is a guide to run the files on a windows client.

Open two instances of command prompt // terminal
get command prompt // terminal to the location of the client and server files.
run the command on both clients: javac *.javac
then run the server first with the command: java MainServer.java
Then run the client with the command: java main.java